name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install mise
        uses: jdx/mise-action@v2

      - name: Fmt check
        run: mise run fmt-check

      - name: Clippy
        run: mise run clippy

      - name: Check
        run: mise run check

      - name: Test
        run: mise run test
